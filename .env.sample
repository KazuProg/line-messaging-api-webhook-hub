# LINE Developers で発行したチャネルシークレット（署名検証に必須）
LINE_CHANNEL_SECRET=your_channel_secret_here

# 以下は任意（未設定時はアプリのデフォルト値を使用）
# DB_PATH=/data/webhook.db
# PORT=8080

# Cloudflare Tunnel（名前付きトンネル用）。未設定時は cloudflared はクイックトンネルで起動します。
# CLOUDFLARE_TUNNEL_TOKEN=
